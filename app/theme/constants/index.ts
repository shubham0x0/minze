import Constants from 'expo-constants';
export { colors as Colors, Theme } from './colors';
import Layout from './layout';
const statusBarHeight = Constants.statusBarHeight;
export { statusBarHeight, Layout };
